{
  "scripts": {
    "test": "echo \"No tests\"",
    "build": "echo \"No root build configured. Run 'npm --prefix dashboard install && npm --prefix dashboard run build' to build the dashboard.\""
  },
  "dependencies": {
    "axios": "^1.11.0",
    "cors": "^2.8.5",
    "dayjs": "^1.11.13",
    "dotenv": "^17.2.1",
    "ioredis": "^5.7.0",
    "pg": "^8.16.3"
  }
}
